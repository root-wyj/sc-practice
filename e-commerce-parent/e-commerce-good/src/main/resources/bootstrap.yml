spring:
  application:
    name: e-commerce-good
server:
  port: 10001

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
